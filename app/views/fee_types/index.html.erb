<h1>Listing fee_types</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fee_types.each do |fee_type| %>
      <tr>
        <td><%= fee_type.title %></td>
        <td><%= fee_type.amount %></td>
      </tr>
    <% end %>
  </tbody>
</table>
