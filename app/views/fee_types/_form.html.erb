<%= form_for(@fee_type) do |f| %>
  <% if @fee_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fee_type.errors.count, "error") %> prohibited this fee_type from being saved:</h2>

      <ul>
      <% @fee_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
